#include "shell.h"

/**
 * free_everything - free command structs before exiting
 * @head: head of command linked list
 **/
 void free_everything(command_t *head)
 {
	 /* Must free path and node itself */
 }
