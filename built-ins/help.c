#include "../shell.h"

/**
 * builtin_help - custom help builtin
 * @args: arguments (string array)
 * Return: exit status
 **/
int builtin_help(char **args)
{
	int status = 0;

	(void)args;
	return (status);
}
