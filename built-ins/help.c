#include "../shell.h"

/**
 * builtin_help - custom help builtin
 * @path: path (string)
 * @args: arguments (string array)
 * Return: exit status
 **/
 int builtin_help(char *path, char **args)
 {
     int status = 0;
     
     return (status);
 }
