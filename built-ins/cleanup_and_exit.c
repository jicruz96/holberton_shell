#include "../shell.h"

/**
 * clean_and_exit - custom exit built-in
 * @path: path (string)
 * @args: arguments (string array)
 * Return: exit status
 **/
int builtin_exit(char *path, char **args)
{
    int status;
    
    return (status);
}
