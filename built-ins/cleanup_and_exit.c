#include "../shell.h"

/**
 * clean_and_exit - custom exit built-in
 * @command: command node
 * Return: exit status
 **/
int clean_and_exit(command_t *command)
{
    int status;
    
    return (status);
}
