#include "../shell.h"

/**
 * builtin_alias - custom alias built-in
 * @args: argument array
 * Return: exit status
 **/
int builtin_alias(char **args)
{
	int status = 0;

	(void)args;
	return (status);
}
