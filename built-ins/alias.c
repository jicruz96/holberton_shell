#include "../shell.h"

/**
 * builtin_alias - custom alias built-in
 * @command: command node
 * Return: exit status
 **/
int builtin_alias(command_t *command)
{
    int status;
    
    return (status);
}
