#include "../shell.h"

/**
 * builtin_alias - custom alias built-in
 * @path: string
 * @args: string array
 * Return: exit status
 **/
int builtin_alias(char *path, char **args)
{
    int status = 0;
    
    return (status);
}
