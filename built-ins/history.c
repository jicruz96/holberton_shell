#include "../shell.h"

/**
 * builtin_history - custom history built-in
 * @command: command node
 * Return: exit status
 **/
int builtin_history(command_t *command)
{
    int status;
    
    return (status);
}
