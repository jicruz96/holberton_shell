#include "shell.h"

/**
 * get_executor - returns executor function in accordance with command's logic
 * @command: command struct
 * Return: exec_f function
 **/
exec_f *get_executor(command_t *command)
{
	exec_f *function;

	return (function);
}
